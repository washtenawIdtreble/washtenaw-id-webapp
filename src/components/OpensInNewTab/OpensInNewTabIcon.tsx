import React from "react";
import icon from "./new-tab.svg";

export const OpensInNewTabIcon = () => {
    return (<img alt={"opens in a new tab"} src={icon} className={"opens-in-new-tab-icon"}/>);
};
